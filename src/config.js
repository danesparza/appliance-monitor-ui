const appconfig = {
  /* Specify service base host & port like this: cerdo.lan:3030 */
  "serviceBaseHostPort" : "localhost:3030" /* Leave blank for localhost/local service port. */
};

export default appconfig;